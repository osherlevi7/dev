# Move Bucket from one to another.

### create virtual env.

``` bash
    python3 -m venv venv 
    source venv/bin/activate
```

### Install the required packages.

``` bash
    python3 -m pip install -r requirements.txt
```



